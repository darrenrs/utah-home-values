"""Planned validation coverage for generated website data.

TODO: Add validators and implement these tests:

- A valid minimal dataset passes.
- A missing required field fails.
- A negative or non-numeric price fails.
- An invalid date fails.
- A duplicate geography and date fails.
- An empty dataset fails.
- An unexpectedly large record-count drop fails.
- A missing manifest target fails.
- Invalid pipeline output does not overwrite existing published files.
- Identical inputs produce deterministic dataset files.
"""
